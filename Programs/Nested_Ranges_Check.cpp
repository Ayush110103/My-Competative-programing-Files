
#include <bits/stdc++.h>
using namespace std;
int main()
{
  int n;
  cin >> n;
  int x, y;
  
  vector<vector<int>> l;
  
  // int x;
  for (int i = 0; i < n;i++){
    cin >> x >> y;
    l.push_back({x, y});
  }
  sort(l.begin(), l.end());


  
}
